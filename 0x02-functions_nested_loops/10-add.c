#include "main.h"
#include <stdio.h>

/**
 * main - adds two integers and gives resutl
 * @a: int to be added to b
 * @b: int to be added to a
 * 
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
