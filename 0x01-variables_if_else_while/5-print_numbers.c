#include <stdio.h>

/**
 * main - Prints all single digits from base 10 ,starts with 0.
 *
 * Return: Always 0.
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\");

	return (0);
}
